name: CI

on:
  push:
    branches:
      - main
      - master

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  tests:
    uses: ./.github/workflows/test.yml

  documentation:
    uses: ./.github/workflows/doc.yml
    needs: [tests]
