{
  "name": "bibliotheque-e2e",
  "version": "1.0.0",
  "description": "Tests end-to-end pour Bibliotheque Spring Angular",
  "scripts": {
    "start:backend": "start-backend.cmd",
    "start:frontend": "npm --prefix \"../Bibliotheque-Front\" start",
    "cy:open": "start-server-and-test start:backend http://localhost:8080 \"start-server-and-test start:frontend http://localhost:4200 cypress open\"",
    "cy:run": "start-server-and-test start:backend http://localhost:8080 \"start-server-and-test start:frontend http://localhost:4200 cypress run\"",
    "cy:run:chrome": "cypress run --browser chrome",
    "cy:run:firefox": "cypress run --browser firefox",
    "cy:run:headless": "cypress run --headless",
    "cy:run:record": "cypress run --record"
  },
  "devDependencies": {
    "cypress": "^14.0.0",
    "start-server-and-test": "^2.1.2",
    "typescript": "^5.3.0"
  }
}
